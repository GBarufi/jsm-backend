using System.ComponentModel.DataAnnotations;

namespace JSM.Domain.Enums
{
    public enum UserGender
    {
        [Display(Name = "Male")]
        M,

        [Display(Name = "Female")]
        F
    }
}
