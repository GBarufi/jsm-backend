using System.ComponentModel.DataAnnotations;

namespace JSM.Domain.Enums
{
    public enum CustomerGender
    {
        [Display(Name = "Male")]
        M,

        [Display(Name = "Female")]
        F
    }
}
